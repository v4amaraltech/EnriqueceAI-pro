# Story 6.1: Auto-registrar Ligações Externas do API4COM

## Status
Ready

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "lint"]

## Story
**As a** gestor de vendas,
**I want** que ligações feitas pelo SDR fora da plataforma (direto no webphone API4COM) sejam automaticamente registradas como atividades no Enriquece AI,
**so that** eu tenha visibilidade completa da produtividade real do time e o histórico do lead fique completo.

## Complexity
**M — Medium** (7 tasks, webhook extension + phone matching + cadence integration + UI badge)

## Scope

**IN:**
- Processar ligações outgoing externas via webhook API4COM
- Matching de lead por telefone (3 fontes)
- Registro de ligação órfã quando lead não encontrado
- Avanço automático de cadência (step phone)
- Badge visual "Externa" na timeline
- Mapeamento ramal → user_id via migration

**OUT:**
- Ligações incoming (lead → SDR) — não processadas
- UI para gerenciar mapeamento de ramais (feito via migration/seed)
- UI para visualizar/gerenciar ligações órfãs (backlog futuro)
- Edição manual de ligações externas
- Notificação push para o SDR sobre ligação registrada

## Dependencies
- Webhook API4COM funcional e recebendo eventos `channel-hangup`
- Tabela `calls` e `interactions` existentes
- Mapeamento ramal → user_id (criado na Task 2 desta story)

## Risks

| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| Falso positivo no matching (número compartilhado entre leads) | Média | Médio | Usar match exato nos últimos 8+ dígitos; se múltiplos leads, vincular ao mais recente (último `updated_at`) |
| Performance da busca em JSONB `socios[].celulares[]` | Baixa | Baixo | Busca ocorre no webhook (async), não impacta UI. Se necessário, criar GIN index |
| Ramal não mapeado no banco | Alta (novos SDRs) | Médio | Criar call órfã sem `performed_by`, logar warning para o gestor revisar |
| API4COM não envia `direction` no payload | Baixa | Alto | Inferir outgoing pelo `callerIdNumber` ser um ramal interno (4 dígitos) |

## Acceptance Criteria

1. Quando o webhook API4COM recebe um `channel-hangup` de uma ligação outgoing que **não** tem call registrada no Enriquece AI, o sistema busca o lead pelo número de destino
2. A busca de lead por telefone deve verificar: `leads.telefone`, `leads.phones[]` (JSONB), e `leads.socios[].celulares[]`
3. Se encontrar o lead, cria automaticamente uma `interaction` do tipo `sent` no canal `phone` com metadata `{ source: 'external_api4com' }`
4. Se **não** encontrar o lead, cria um registro de ligação "órfã" na tabela `calls` com `lead_id: null` para revisão do gestor
5. Apenas ligações **outgoing** (SDR → lead) são processadas. Incoming são ignoradas
6. Se o lead está em uma cadência ativa com próximo step = `phone`, a ligação externa **avança o step** automaticamente
7. Na timeline do lead, ligações externas exibem indicador visual "Via API4COM (externa)" para distinguir de ligações feitas pela plataforma
8. A normalização de telefone para matching usa a mesma lógica existente (`normalizePhone` — remove DDI, DDD, formatação)
9. O SDR é identificado pelo `ramal` do webhook, mapeado via `api4com_extension` no `organization_members` ou configuração equivalente

## Tasks / Subtasks

- [ ] **Task 1**: Criar função de matching de lead por telefone (AC: 2, 8)
  - [ ] 1.1 Implementar `findLeadByPhone(orgId, phone)` em `src/features/calls/services/`
  - [ ] 1.2 Buscar em `leads.telefone` (normalizado)
  - [ ] 1.3 Buscar em `leads.phones[]` JSONB (normalizado)
  - [ ] 1.4 Buscar em `leads.socios[].celulares[]` (normalizado)
  - [ ] 1.5 Retornar primeiro match encontrado (prioridade: telefone > phones > socios)
  - [ ] 1.6 Testes unitários para a função de matching

- [ ] **Task 2**: Mapear ramal → user_id do SDR (AC: 9)
  - [ ] 2.1 Verificar se já existe campo `api4com_extension` em `organization_members` ou tabela de configuração
  - [ ] 2.2 Se não existir, criar migration para armazenar mapeamento ramal → user_id
  - [ ] 2.3 Implementar `findUserByExtension(orgId, extension)` 

- [ ] **Task 3**: Processar ligações externas no webhook API4COM (AC: 1, 4, 5)
  - [ ] 3.1 No handler de `channel-hangup`, detectar se a call já existe no banco (ligação interna) ou não (ligação externa)
  - [ ] 3.2 Se externa + outgoing: chamar `findLeadByPhone()` com o número de destino
  - [ ] 3.3 Se encontrou lead: criar `interaction` + `call` vinculados
  - [ ] 3.4 Se não encontrou lead: criar `call` com `lead_id: null` (órfã)
  - [ ] 3.5 Ignorar ligações incoming silenciosamente

- [ ] **Task 4**: Criar interaction automática para ligações externas (AC: 3, 7)
  - [ ] 4.1 Inserir na tabela `interactions` com `type: 'sent'`, `channel: 'phone'`, `performed_by: userId`
  - [ ] 4.2 Incluir `metadata: { source: 'external_api4com', api4com_id: body.id, duration: payload.duration }`
  - [ ] 4.3 Garantir que o `message_content` inclui info da ligação (duração, resultado)

- [ ] **Task 5**: Avançar cadência quando aplicável (AC: 6)
  - [ ] 5.1 Após criar interaction, verificar se lead tem `cadence_enrollment` ativa
  - [ ] 5.2 Se próximo step é `phone`, avançar enrollment para próximo step
  - [ ] 5.3 Reutilizar lógica existente de avanço de step (ex: `advanceEnrollmentStep`)

- [ ] **Task 6**: Indicador visual "Via API4COM (externa)" na timeline (AC: 7)
  - [ ] 6.1 No `LeadTimelineTab`, verificar `metadata.source === 'external_api4com'`
  - [ ] 6.2 Exibir badge/tag "Externa" ao lado do item da timeline
  - [ ] 6.3 Manter estilo consistente com os badges existentes

- [ ] **Task 7**: Testes
  - [ ] 7.1 Teste unitário: `findLeadByPhone` com match em cada fonte (telefone, phones, socios)
  - [ ] 7.2 Teste unitário: `findUserByExtension` 
  - [ ] 7.3 Teste unitário: webhook processa ligação externa corretamente
  - [ ] 7.4 Teste unitário: ligação órfã criada quando lead não encontrado
  - [ ] 7.5 Teste unitário: ligação incoming ignorada

## Dev Notes

### Arquivos Relevantes
- `src/app/api/webhooks/api4com/route.ts` — webhook handler principal (processApi4ComEvent, updateCallFromWebhook)
- `src/features/calls/actions/initiate-api4com-call.ts` — cria call interna (referência de como registrar)
- `src/features/activities/components/ActivityPhonePanel.tsx` — UI de ligação (referência de flow)
- `src/features/calls/hooks/use-call-hangup-detection.ts` — detecção de hangup via Realtime
- `src/features/cadences/actions/execute-cadence.ts` — lógica de avanço de step na cadência
- `src/features/leads/components/LeadTimelineTab.tsx` — timeline do lead (onde adicionar badge)
- `src/shared/utils/phone.ts` ou similar — funções de normalização de telefone

### Fluxo Atual do Webhook
1. API4COM envia `channel-hangup` → `route.ts` processa
2. Busca call existente por `api4com_call_id` em `calls`
3. Se encontra: atualiza com duração, status, recording
4. Se **não** encontra: atualmente ignora → **este é o ponto de extensão**

### Normalização de Telefone
Usar `normalizePhone()` existente que remove: `+55`, `(`, `)`, `-`, espaços. Comparar últimos 8-9 dígitos para evitar problemas com/sem DDD/DDI.

### Mapeamento Ramal → SDR
O webhook envia `callerIdNumber` (ramal do SDR, ex: "1033") e `destination` (número do lead). Precisamos mapear "1033" → user_id do Guilherme Marques.

Dados do API4COM dashboard:
| Nome | Ramal |
|------|-------|
| lucas.carceliano | 1038 |
| jhonata.banqueri | 1026 |
| deborah.karoline | 1035 |
| Vinicius Mercante | 1014 |
| Paulo Millesoli | 1030 |
| Matheus Martins | 1028 |
| Ismael Dobelin | 1024 |
| Guilherme Marques | 1033 |

### Testing

- **Framework**: Vitest
- **Localização**: colocated em `src/features/calls/services/*.test.ts`
- **Mocking**: `vi.mock('@/lib/supabase/server')` para Supabase
- **Padrão**: seguir `src/features/statistics/services/feedback-analytics.service.test.ts` como referência

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: Integration (API4COM webhook extension)
**Secondary Type(s)**: API (business logic), Frontend (timeline badge)
**Complexity**: Medium

### Specialized Agent Assignment
**Primary Agents**:
- @dev: Implementation de toda a lógica
- @qa: Validação de testes e edge cases

**Supporting Agents**:
- @architect: Revisão do pattern de matching de telefone

### Quality Gate Tasks
- [ ] Pre-Commit (@dev): Run before marking story complete
- [ ] Pre-PR (@github-devops): Run before creating pull request

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: [CRITICAL, HIGH]

**Predicted Behavior**:
- CRITICAL issues: auto_fix
- HIGH issues: auto_fix (iteration < 2) else document_as_debt

### CodeRabbit Focus Areas
**Primary Focus**:
- Webhook handler error handling (silent failures)
- Phone number normalization edge cases (DDI, DDD, formatos)

**Secondary Focus**:
- Cadence step advancement correctness
- Timeline UI consistency

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-04-23 | 1.0 | Story criada | @sm (River) |
| 2026-04-23 | 1.1 | Adicionado Scope, Complexity, Dependencies, Risks (validação @po) | @po (Pax) |

## Dev Agent Record

### Agent Model Used
_To be filled by @dev_

### Debug Log References
_To be filled by @dev_

### Completion Notes List
_To be filled by @dev_

### File List
_To be filled by @dev_

## QA Results
_To be filled by @qa_
