# Story 4.7: Billing Polish

## Status

**Draft**

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test", "build"]

## Complexity Estimate

**S (3 points)** — Integração com Stripe API para faturas, método de pagamento, e UX polish com toasts.

## Risks

- **R1:** Stripe API para invoices pode retornar muitos dados — mitigado com limit e paginação
- **R2:** Método de pagamento pode não existir para trials — mitigado com empty state

## Dependencies

- **Story 4.1** — Planos devem existir para billing page funcionar completamente

## Story

**As a** manager,
**I want** to see my invoice history, payment method, and clear feedback after checkout,
**so that** I have full visibility into my billing and feel confident about my subscription.

## Scope

**IN:**
- Seção "Histórico de Faturas" na billing page (via Stripe API)
- Exibição do método de pagamento atual (tipo + últimos 4 dígitos)
- Banner/toast de sucesso após checkout (`?success=true`)
- Banner/toast de cancelamento após checkout (`?canceled=true`)
- Empty states adequados para trial (sem faturas, sem pagamento)
- Link para Stripe Customer Portal

**OUT:**
- Customização de método de pagamento fora do Stripe Portal
- Boleto/PIX como métodos de pagamento
- Nota fiscal brasileira (NF-e)
- Download de faturas no app (usa PDF link do Stripe)

## Acceptance Criteria

1. Seção "Histórico de Faturas" mostra últimas 10 faturas com: data, valor (BRL), status (pago/pendente/falhou), link para PDF
2. Se não há faturas (trial), mostra empty state: "Nenhuma fatura ainda. As faturas aparecerão aqui após o primeiro pagamento."
3. Método de pagamento atual visível: tipo (Visa/Mastercard/etc) + últimos 4 dígitos
4. Se não há método de pagamento (trial), mostra: "Nenhum método de pagamento cadastrado"
5. Banner verde "Plano atualizado com sucesso!" quando URL tem `?success=true`
6. Banner amarelo "Checkout cancelado. Seu plano não foi alterado." quando URL tem `?canceled=true`
7. Link "Gerenciar pagamento" abre Stripe Customer Portal
8. `pnpm typecheck`, `pnpm lint` e `pnpm test:run` passam sem erros novos

## Tasks / Subtasks

- [ ] **Task 1: Server Action — Fetch Invoices** (AC: 1, 2)
  - [ ] 1.1 Criar `fetchInvoices()` em `src/features/billing/actions/`
  - [ ] 1.2 Se `stripe_customer_id` existe: `stripe.invoices.list({ customer, limit: 10 })`
  - [ ] 1.3 Mapear para tipo `Invoice`: { date, amount, status, pdfUrl }
  - [ ] 1.4 Se não tem customer_id: retornar array vazio

- [ ] **Task 2: Server Action — Fetch Payment Method** (AC: 3, 4)
  - [ ] 2.1 Criar `fetchPaymentMethod()` em `src/features/billing/actions/`
  - [ ] 2.2 `stripe.customers.retrieve(customerId, { expand: ['default_source', 'invoice_settings.default_payment_method'] })`
  - [ ] 2.3 Retornar: `{ brand, last4, expMonth, expYear }` ou null

- [ ] **Task 3: Componente InvoiceHistory** (AC: 1, 2)
  - [ ] 3.1 Criar `src/features/billing/components/InvoiceHistory.tsx`
  - [ ] 3.2 Tabela: Data | Valor | Status (badge) | Ação (link PDF)
  - [ ] 3.3 Empty state para sem faturas
  - [ ] 3.4 Status badges: "Pago" (verde), "Pendente" (amarelo), "Falhou" (vermelho)

- [ ] **Task 4: Componente PaymentMethod** (AC: 3, 4)
  - [ ] 4.1 Criar `src/features/billing/components/PaymentMethod.tsx`
  - [ ] 4.2 Exibir: ícone do brand (Visa/MC) + "•••• {last4}" + "Expira {mm/yy}"
  - [ ] 4.3 Empty state para sem método de pagamento
  - [ ] 4.4 Link "Gerenciar pagamento" → Stripe Portal

- [ ] **Task 5: Checkout Feedback** (AC: 5, 6)
  - [ ] 5.1 Na billing page, detectar query params `success` e `canceled`
  - [ ] 5.2 Renderizar banner/toast apropriado
  - [ ] 5.3 Usar `useSearchParams()` + `router.replace()` para limpar params

- [ ] **Task 6: Integrar na Billing Page** (AC: 1, 3, 7)
  - [ ] 6.1 Adicionar `InvoiceHistory` e `PaymentMethod` na página de billing
  - [ ] 6.2 Fetch dados em paralelo com billing overview existente
  - [ ] 6.3 Layout: Plano Atual → Consumo (4.4) → Pagamento → Faturas → Comparação de Planos

- [ ] **Task 7: Testes** (AC: 8)
  - [ ] 7.1 Testes para InvoiceHistory (rendering, empty state)
  - [ ] 7.2 Testes para PaymentMethod (rendering, empty state)
  - [ ] 7.3 Testes para checkout feedback (toasts)

## Dev Notes

**Stripe Invoice object (campos relevantes):**
```typescript
{
  created: number;          // Unix timestamp
  amount_paid: number;      // Em centavos (BRL)
  status: 'paid' | 'open' | 'void' | 'uncollectible';
  invoice_pdf: string;      // URL do PDF
  hosted_invoice_url: string; // URL da fatura online
}
```

**Stripe Payment Method:**
```typescript
{
  card: {
    brand: 'visa' | 'mastercard' | 'amex' | 'elo';
    last4: string;
    exp_month: number;
    exp_year: number;
  }
}
```

**Layout final da billing page:**
```
┌─────────────────────────────────┐
│ Banner success/canceled (temp)  │
├─────────────────────────────────┤
│ Plano Atual (BillingView)       │
├─────────────────────────────────┤
│ Consumo (UsageDashboard - 4.4)  │
├─────────────────────────────────┤
│ Pagamento (PaymentMethod)       │
├─────────────────────────────────┤
│ Histórico de Faturas            │
├─────────────────────────────────┤
│ Comparação de Planos            │
└─────────────────────────────────┘
```

### Testing

- **Framework:** Vitest + Testing Library
- **Location:** `src/features/billing/components/`, `src/features/billing/actions/`
- **Rodar:** `pnpm vitest run src/features/billing`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-03 | 1.0 | Story criada a partir do Epic 4 | @pm (Morgan) |
