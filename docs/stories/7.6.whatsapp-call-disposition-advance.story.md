# Story 7.6: Disposition → Avanço/Callback da Cadência

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "lint"]

## Story
**As a** SDR,
**I want** registrar o resultado da ligação (disposition) ao desligar e ter a cadência reagir de acordo,
**so that** a atividade feche corretamente e o lead avance, reagende ou faça callback sem ação manual extra.

## Complexity
**M — Medium** (mapa de disposition + action nova de reschedule)

## Scope
**IN:**
- Captura de disposition (enum `call_status`: `significant | not_significant | no_contact | busy | not_connected`) ao `ended`.
- Mapa disposition → ação na cadência (§E do plano).
- **Callback** ("reagendou"): action nova "reagendar step atual" que seta `next_step_due` (horário escolhido pelo SDR), mantém `current_step`/`status='active'`, **sem** chamar `advance_enrollment_after_step`.
- Demais dispositions: fecham a atividade e avançam via `advance_enrollment_after_step`.

**OUT:**
- Persistência da call/interaction (7.7) — esta story consome o resultado e dispara avanço.
- Gravação (7.8).

## Dependencies
- 7.5 (painel emite o evento `ended` + dados da chamada).
- RPC `advance_enrollment_after_step` (existe).

## Risks
| Risco | Prob. | Impacto | Mitigação |
|-------|-------|---------|-----------|
| Avanço duplicado (clique duplo/retry) | Média | Médio | reusar idempotência do `advance_enrollment_after_step` |
| `not_connected` contar como tentativa | Média | Baixo | `not_connected` (erro técnico) NÃO fecha a atividade; reabre na fila |
| Reschedule sem horário válido | Baixa | Baixo | validar `next_step_due` futuro |

## Acceptance Criteria
1. Ao `ended`, abre captura de disposition com os 5 valores de `call_status`.
2. `significant` / `not_significant`: fecha a atividade e avança a cadência (`advance_enrollment_after_step`).
3. `busy` / `no_contact`: fecha a tentativa e reabre/reagenda conforme política de tentativas.
4. `not_connected` (erro técnico): **não** conta como tentativa; a atividade volta pra fila.
5. Disposition "reagendou/callback": SDR escolhe data/hora → action nova seta `next_step_due` no enrollment, mantém `current_step` e `status='active'`, sem avançar.
6. Disposition gravada **fecha** a atividade correspondente (exceto `not_connected`).
7. Sem regressão no fluxo de disposition da ligação comum (API4COM).
8. Testes cobrem cada ramo do mapa + idempotência do avanço.

## Tasks / Subtasks
- [x] **Task 1**: UI de disposition pós-chamada — `CallDispositionForm` (radio dos 5 `call_status` + date-picker condicional de callback). Componente **standalone reusável**; a 7.5 monta ao desligar (não mexe nos painéis existentes)
- [x] **Task 2**: Action "reagendar step atual" — `reschedule-current-step.ts` (seta `next_step_due` no horário escolhido, mantém `current_step`/`status='active'`, sem avançar)
- [x] **Task 3**: Mapa disposition→ação (`disposition.ts`) + orquestração `apply-call-disposition.ts` (advance via RPC / reschedule / none)
- [x] **Task 4**: `not_connected` → `none` (no-op; a atividade volta para a fila, não fecha)
- [x] **Task 5**: 12 testes (mapa, reschedule, orquestração de cada ramo). Idempotência herdada do `advance_enrollment_after_step` (row-lock)

## Dev Notes
- Decisão travada: callback = reabre o mesmo step (opção a). Mapa completo em §E do plano.
- **Política de tentativas (resolve o GO-condicional do @po):** `significant`/`not_significant` → avança; `busy`/`no_contact` → reagenda (callback no horário do SDR); `not_connected` → none. Definida em `mapDispositionToAction`.
- `applyCallDisposition` **não** grava `calls.status` — a persistência da chamada é da 7.7. Aqui só a ação de cadência.
- Separação de risco: nada wira no `ActivityPhonePanel`/fluxo de ligação atual; o componente é montado pela 7.5.

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (@dev / Dex)

### File List
- `src/features/whatsapp-calls/disposition.ts` (novo — mapa puro + opções)
- `src/features/whatsapp-calls/disposition.test.ts` (novo)
- `src/features/whatsapp-calls/actions/apply-call-disposition.ts` (novo — orquestração)
- `src/features/whatsapp-calls/actions/apply-call-disposition.test.ts` (novo)
- `src/features/whatsapp-calls/components/CallDispositionForm.tsx` (novo — UI reusável)
- `src/features/activities/actions/reschedule-current-step.ts` (novo — action de callback)
- `src/features/activities/actions/reschedule-current-step.test.ts` (novo)

### Completion Notes
- `typecheck` ✓ · `lint` ✓ · **suíte completa 1441 testes** ✓ (10 RLS skipped). 12 testes novos.
- **Dependência 7.5:** o `CallDispositionForm` é standalone/testável; o wiring no "ended" da chamada (montar o form quando a chamada WebRTC encerra) é da 7.5. Não toquei nos painéis existentes (não mexer no que funciona).
- `reschedule-current-step` difere do `skip-activity` (snooze fixo) por aceitar o horário exato; valida futuro + enrollment ativo.
- CodeRabbit pulado (CLI WSL-only no macOS) — gate = typecheck/lint/test + CI.

## Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-06-28 | @dev | Story criada a partir do epic-7 |
| 2026-06-28 | @po (Pax) | Validada (10-point checklist) 8/10 — GO condicional. Status Draft → Ready. Refinar: quantificar política de tentativas (retries p/ busy/no_contact). |
| 2026-06-29 | @dev (Dex) | Implementada: mapa + reschedule + orquestração + form reusável + 12 testes. Política de tentativas definida. typecheck/lint/1441 ✓. Status Ready → Ready for Review. |
