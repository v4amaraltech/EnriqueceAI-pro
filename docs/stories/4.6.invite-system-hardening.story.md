# Story 4.6: Invite System Hardening

## Status

**Done**

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test", "build"]

## Complexity Estimate

**S (3 points)** — Remover exposição de senha, usar magic link, adicionar gestão de convites pendentes.

## Risks

- **R1:** Magic link pode não funcionar se Supabase email não estiver configurado — mitigado com fallback para fluxo atual
- **R2:** Expiração de convites pode bloquear membros legítimos — mitigado com opção de reenvio

## Dependencies

- Nenhuma dependência forte — pode ser implementada em paralelo com Wave 1

## Story

**As a** manager,
**I want** a secure invite system without exposed passwords and with invite management,
**so that** I can safely onboard team members and track pending invitations.

## Scope

**IN:**
- Remover exibição de senha temporária do `InviteMemberDialog`
- Convite via Supabase Auth magic link (email de convite padrão)
- Lista de convites pendentes na página de usuários
- Ações: reenviar convite, revogar convite
- Expiração de convites após 7 dias
- Migration: coluna `invited_expires_at` em `organization_members`

**OUT:**
- Email template customizado com branding EnriqueceAI
- Convite via link compartilhável (share link)
- Bulk invite (CSV de emails)
- Convite via WhatsApp

## Acceptance Criteria

1. `InviteMemberDialog` não mostra senha temporária após enviar convite
2. Dialog mostra mensagem "Convite enviado para {email}" com ícone de sucesso
3. Membro convidado recebe email com link de acesso (Supabase Auth invite)
4. Lista de convites pendentes visível na página `/settings/users` (status 'invited')
5. Manager pode reenviar convite pendente (botão "Reenviar")
6. Manager pode revogar convite pendente (botão "Revogar" → status 'removed')
7. Convites com mais de 7 dias expiram automaticamente (cron ou check on-demand)
8. `pnpm typecheck`, `pnpm lint` e `pnpm test:run` passam sem erros novos

## Tasks / Subtasks

- [x] **Task 1: Migration** (AC: 7)
  - [x] 1.1 Criar migration: `ALTER TABLE organization_members ADD COLUMN invited_expires_at TIMESTAMPTZ`
  - [x] 1.2 Atualizar tipo `OrganizationMember` com nova coluna

- [x] **Task 2: Refatorar invite-member.ts** (AC: 1, 2, 3)
  - [x] 2.1 Remover retorno de senha temporária da action
  - [x] 2.2 Setar `invited_expires_at = now() + INTERVAL '7 days'`
  - [x] 2.3 Usar `admin.auth.admin.inviteUserByEmail()` com `redirectTo` configurado
  - [x] 2.4 Retornar `ActionResult<{ email: string }>` em vez de `ActionResult<{ email: string; tempPassword: string }>`

- [x] **Task 3: Refatorar InviteMemberDialog** (AC: 1, 2)
  - [x] 3.1 Remover exibição de senha temporária
  - [x] 3.2 Mostrar mensagem de sucesso: "Convite enviado para {email}"
  - [x] 3.3 Remover copy de senha

- [x] **Task 4: Lista de Convites Pendentes** (AC: 4)
  - [x] 4.1 Na página de usuários, seção separada "Convites Pendentes"
  - [x] 4.2 Listar membros com status 'invited': email, role, data do convite, tempo restante
  - [x] 4.3 Indicador visual de tempo restante (ex: "Expira em 3 dias")

- [x] **Task 5: Ações de Gestão** (AC: 5, 6)
  - [x] 5.1 Botão "Reenviar" — chama `admin.auth.admin.inviteUserByEmail()` novamente e reseta `invited_expires_at`
  - [x] 5.2 Botão "Revogar" — seta status para 'removed'
  - [x] 5.3 Server actions: `resendInvite(memberId)` e `revokeInvite(memberId)`

- [x] **Task 6: Expiração de Convites** (AC: 7)
  - [x] 6.1 Expiração on-demand na página `/settings/users`: `WHERE status = 'invited' AND invited_expires_at < now()`
  - [x] 6.2 Auto-ativação de membros convidados no `layout.tsx` (status 'invited' → 'active' ao logar)

- [x] **Task 7: Testes** (AC: 8)
  - [x] 7.1 Testes para invite-member (sem senha no retorno, status 'invited', invited_expires_at)
  - [x] 7.2 Testes para existing user (status 'active', upsert com onConflict)
  - [x] 7.3 Testes para validation errors e redirect

## Dev Notes

**Fluxo atual (a ser removido):**
```typescript
// invite-member.ts retorna:
return { success: true, data: { email, tempPassword: 'Enriqueceai123' } };
// InviteMemberDialog mostra a senha no UI
```

**Fluxo novo:**
```typescript
// invite-member.ts retorna:
return { success: true, data: { email } };
// InviteMemberDialog mostra: "Convite enviado para {email}"
```

**Supabase invite:**
```typescript
await supabaseAdmin.auth.admin.inviteUserByEmail(email, {
  redirectTo: `${APP_URL}/auth/callback`,
  data: { org_id: orgId, role }
});
```

### Testing

- **Framework:** Vitest + Testing Library
- **Location:** `src/features/auth/actions/`, `src/features/auth/components/`
- **Rodar:** `pnpm vitest run src/features/auth`

## File List

| File | Action | Description |
|------|--------|-------------|
| `supabase/migrations/20260303130000_add_invited_expires_at.sql` | Created | Migration: add invited_expires_at column |
| `src/features/auth/types/index.ts` | Modified | Add invited_expires_at to OrganizationMemberRow |
| `src/features/auth/actions/invite-member.ts` | Modified | Remove temp password, use magic link, status 'invited' |
| `src/features/auth/components/InviteMemberDialog.tsx` | Modified | Success toast with email, remove password display |
| `src/features/auth/actions/resend-invite.ts` | Created | Server action to resend invite + reset expiry |
| `src/features/auth/actions/revoke-invite.ts` | Created | Server action to revoke pending invite |
| `src/features/auth/components/UserManagement.tsx` | Modified | Pending invites section, resend/revoke buttons |
| `src/app/(app)/settings/users/page.tsx` | Modified | On-demand invite expiration, filter removed |
| `src/app/(app)/layout.tsx` | Modified | Auto-activate invited members on login |
| `src/features/auth/actions/invite-member.test.ts` | Modified | Updated tests for new invite flow |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Debug Log References
N/A

### Completion Notes
- Removed hardcoded temp password `Enriqueceai123` — now uses Supabase magic link via `inviteUserByEmail()`
- New member gets status `invited` (not `active`) with 7-day expiry
- Existing users are added directly with `active` status via upsert
- On-demand expiration chosen over cron (simpler, no infrastructure needed)
- Auto-activation in layout.tsx: when invited user clicks magic link and enters app, status flips to `active`
- `handle_new_user` trigger auto-org is cleaned up by deleting the auto-created org
- All 54 auth tests pass, 0 new lint errors, typecheck clean

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-03 | 1.0 | Story criada a partir do Epic 4 | @pm (Morgan) |
| 2026-03-03 | 1.1 | Implementation complete — all tasks done | @dev (Dex) |
| 2026-03-07 | 1.2 | QA Gate: PASS — 7/7 checks, all ACs verified, 54 tests pass | @qa (Quinn) |
