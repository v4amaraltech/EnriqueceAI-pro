# Story 4.3: Onboarding 2.0 com Checkout

## Status

**InProgress**

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test", "build"]

## Complexity Estimate

**L (8 points)** — Expansão de 3 para 6 steps, integração Stripe Checkout no onboarding, Gmail OAuth, persistência de progresso no banco.

## Risks

- **R1:** Redirect Stripe Checkout → onboarding pode perder contexto — mitigado: salvar step no banco antes do redirect
- **R2:** OAuth Gmail pode falhar silenciosamente — mitigado: step com skip, verificação de conexão ao retornar
- **R3:** Onboarding incompleto deixa org em estado inconsistente — mitigado: heurística de redirecionamento já existe (regex domain name), adicionamos onboarding_step

## Dependencies

- **Story 4.1** (Done) — Plans devem existir para step de seleção de plano
- **Story 4.6** (Done) — Invite system com magic links para step de convite

## Story

**As a** new user signing up for the first time,
**I want** a guided onboarding that lets me choose a plan, connect my email, and invite my team,
**so that** I'm fully set up and productive from day one.

## Scope

**IN:**
- Step 1: Nome da empresa (existente, manter lógica atual)
- Step 2: Seleção de plano (3 cards comparativos, incluindo opção "Continuar com trial gratuito")
- Step 3: Checkout Stripe (redirect para Stripe Checkout, retorno com success/cancel)
- Step 4: Conectar Gmail (OAuth via `getGmailAuthUrl()`, com skip)
- Step 5: Convidar equipe (emails + role, com skip, reutilizar `inviteMember`)
- Step 6: Pronto! (checklist do que foi configurado com checkmarks)
- Progress bar atualizado para 6 steps
- Persistência de step atual em `organizations.onboarding_step`
- Retomar onboarding do último step se interrompido

**OUT:**
- Conectar CRM durante onboarding
- Conectar WhatsApp durante onboarding
- Upload de leads CSV durante onboarding
- Feature tour / guided walkthrough
- Onboarding diferenciado por plano

## Acceptance Criteria

1. Onboarding tem 6 steps com progress bar visual (indicadores de step ativo/completo/pendente)
2. Step 1 coleta nome da empresa (mantém comportamento atual via `completeOnboarding`)
3. Step 2 mostra 3 planos com preços e features (reutiliza dados de `fetchPlanComparison`)
4. Step 3 redireciona para Stripe Checkout; ao retornar com `success`, avança para step 4
5. Step 3 permite "Continuar com trial gratuito" (skip checkout, mantém subscription `trialing`)
6. Step 4 permite conectar Gmail via OAuth ou skip (reutiliza `getGmailAuthUrl`)
7. Step 5 permite convidar membros por email com role selector (reutiliza `inviteMember`)
8. Step 6 mostra resumo do que foi configurado com checkmarks verdes
9. Se onboarding interrompido, retoma do último step ao voltar (via `organizations.onboarding_step`)
10. Migration adiciona coluna `onboarding_step` na tabela `organizations`
11. `pnpm typecheck`, `pnpm lint`, `pnpm test:run` e `pnpm build` passam

## Tasks / Subtasks

- [x] **Task 1: Migration — onboarding_step column** (AC: 10)
  - [x] 1.1 `ALTER TABLE organizations ADD COLUMN onboarding_step INTEGER DEFAULT NULL`
  - [x] 1.2 NULL = onboarding completo, 0-5 = step atual
  - [x] 1.3 Atualizar `handle_new_user()` para setar `onboarding_step = 0` em orgs novas

- [x] **Task 2: Server Action — Save Onboarding Step** (AC: 9)
  - [x] 2.1 Criar `saveOnboardingStep(step: number)` em `src/features/auth/actions/`
  - [x] 2.2 Update `organizations.onboarding_step` para org do user autenticado
  - [x] 2.3 Quando step = null (completo), remover heurística de domain name redirect

- [x] **Task 3: Atualizar Redirect Logic** (AC: 9)
  - [x] 3.1 Em `layout.tsx`, checar `onboarding_step IS NOT NULL` ao invés de regex do nome
  - [x] 3.2 Manter regex como fallback para orgs criadas antes da migration
  - [x] 3.3 Em `completeOnboarding`, setar `onboarding_step = 1` após salvar nome

- [x] **Task 4: Expandir OnboardingWizard — 6 Steps** (AC: 1)
  - [x] 4.1 Atualizar STEPS array para 6 entries com ícones e descrições
  - [x] 4.2 Progress bar com indicadores: completo (check verde), ativo (highlight), pendente (cinza)
  - [x] 4.3 Inicializar step a partir de `onboarding_step` prop (vindo do server)
  - [x] 4.4 Salvar step no banco ao avançar (via `saveOnboardingStep`)

- [x] **Task 5: Step 2 — Seleção de Plano** (AC: 3)
  - [x] 5.1 Criar componente `OnboardingPlanStep`
  - [x] 5.2 Renderizar 3 plan cards (reutilizar estilo de `PlanComparison`)
  - [x] 5.3 Card selecionável com highlight visual
  - [x] 5.4 Botão "Escolher plano" → salva seleção e avança
  - [x] 5.5 Destacar "Trial 14 dias grátis" no plano Starter

- [x] **Task 6: Step 3 — Stripe Checkout** (AC: 4, 5)
  - [x] 6.1 Criar componente `OnboardingCheckoutStep`
  - [x] 6.2 Se plano selecionado != Starter: botão "Ir para pagamento" → `createCheckoutSession` com `return_url = /onboarding?step=2`
  - [x] 6.3 Ao retornar com `?success=true`: avançar para step 3
  - [x] 6.4 Ao retornar com `?canceled=true`: manter no step 2 com mensagem
  - [x] 6.5 Botão "Continuar com trial gratuito" → skip, avançar para step 3
  - [x] 6.6 Se plano = Starter (trial): auto-skip para step 3

- [x] **Task 7: Step 4 — Conectar Gmail** (AC: 6)
  - [x] 7.1 Criar componente `OnboardingGmailStep`
  - [x] 7.2 Botão "Conectar Gmail" → `getGmailAuthUrl()` com redirect para `/onboarding?step=3&gmail=connected`
  - [x] 7.3 Ao retornar: verificar se `gmail_connections` tem registro ativo
  - [x] 7.4 Se conectado: mostrar email conectado com check verde
  - [x] 7.5 Botão "Pular por agora" → avançar para step 4

- [x] **Task 8: Step 5 — Convidar Equipe** (AC: 7)
  - [x] 8.1 Criar componente `OnboardingInviteStep`
  - [x] 8.2 Formulário: email + role (manager/sdr) + botão "Convidar"
  - [x] 8.3 Reutilizar `inviteMember` action
  - [x] 8.4 Lista de convites enviados com status
  - [x] 8.5 Botão "Pular por agora" / "Continuar" → avançar para step 5

- [x] **Task 9: Step 6 — Pronto!** (AC: 8)
  - [x] 9.1 Criar componente `OnboardingCompleteStep`
  - [x] 9.2 Checklist: empresa configurada, plano selecionado, email conectado (ou não), equipe convidada (ou não)
  - [x] 9.3 Itens configurados = check verde, pulados = texto cinza
  - [x] 9.4 Botão "Ir para o dashboard" → setar `onboarding_step = null`, redirect para `/`

- [x] **Task 10: Testes** (AC: 11)
  - [x] 10.1 `pnpm typecheck` — sem erros
  - [x] 10.2 `pnpm build` — sem erros
  - [x] 10.3 `pnpm test:run` — 162/162 testes billing/auth passando (6 falhas pré-existentes em dashboard/templates não relacionadas)
  - [x] 10.4 `pnpm lint` — sem erros novos (6 pré-existentes em LeadInfoPanel.tsx)

## Dev Notes

**Onboarding atual (3 steps):**
- `src/app/onboarding/OnboardingWizard.tsx` — client component, state local
- `src/features/auth/actions/complete-onboarding.ts` — salva nome da org
- Trigger: regex `^[a-z0-9.-]+\.[a-z]{2,}$` no nome da org em `layout.tsx`

**Actions reutilizáveis:**
- `createCheckoutSession(planId)` → retorna `{ url }` do Stripe
- `getGmailAuthUrl()` → retorna URL do OAuth consent
- `inviteMember(formData)` → cria magic link + org_member record
- `fetchPlanComparison()` → retorna planos ativos + plano atual

**Fluxo de redirect Stripe:**
```
Onboarding Step 3 → Stripe Checkout → /onboarding?step=3&success=true → Step 4
                                     → /onboarding?step=3&canceled=true → Step 3
```

**Fluxo Gmail OAuth:**
```
Onboarding Step 4 → Gmail consent → /api/auth/callback/gmail → /onboarding?step=4 → Step 5
```

**Progress tracking:**
```
onboarding_step: 0 = empresa, 1 = plano, 2 = checkout, 3 = gmail, 4 = invite, 5 = pronto, NULL = completo
```

### Testing

- **Framework:** Vitest + Testing Library
- **Location:** `src/app/onboarding/`
- **Rodar:** `pnpm vitest run src/app/onboarding`

## File List

| File | Action | Description |
|------|--------|-------------|
| `supabase/migrations/20260307160000_add_onboarding_step.sql` | Create | Add onboarding_step column to organizations |
| `src/features/auth/types/index.ts` | Modify | Add onboarding_step to OrganizationRow |
| `src/features/auth/actions/save-onboarding-step.ts` | Create | Server action to persist step progress |
| `src/features/auth/actions/complete-onboarding.ts` | Modify | Set onboarding_step = 1, checkNeedsOnboarding returns number|false |
| `src/features/billing/actions/create-checkout.ts` | Modify | Accept optional returnPath for Stripe redirect |
| `src/features/integrations/actions/manage-gmail.ts` | Modify | Accept optional redirectAfter for Gmail OAuth state |
| `src/app/api/auth/callback/gmail/route.ts` | Modify | Check OAuth state param for onboarding redirect |
| `src/app/(app)/layout.tsx` | Modify | Check onboarding_step column with regex fallback |
| `src/app/onboarding/page.tsx` | Modify | Pass initialStep, plans, checkoutSuccess, gmailConnected |
| `src/app/onboarding/OnboardingWizard.tsx` | Modify | Expand to 6 steps with progress persistence |
| `src/app/onboarding/steps/OnboardingPlanStep.tsx` | Create | Plan selection cards |
| `src/app/onboarding/steps/OnboardingCheckoutStep.tsx` | Create | Stripe checkout with onboarding return URL |
| `src/app/onboarding/steps/OnboardingGmailStep.tsx` | Create | Gmail OAuth with onboarding redirect |
| `src/app/onboarding/steps/OnboardingInviteStep.tsx` | Create | Team invite form |
| `src/app/onboarding/steps/OnboardingCompleteStep.tsx` | Create | Configuration checklist |

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-07 | 1.0 | Story criada a partir do Epic 4.3 definition | @sm (River) |
| 2026-03-07 | 1.1 | Validação GO (10/10), status → Ready | @po (Pax) |
| 2026-03-07 | 2.0 | Implementação completa: migration, 6-step wizard, Stripe/Gmail integração, typecheck/build OK | @dev (Dex) |
