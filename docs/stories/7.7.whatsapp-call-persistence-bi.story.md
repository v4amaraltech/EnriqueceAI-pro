# Story 7.7: Persistência de Call + Interaction + Webhook BI

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "lint", "sql verify"]

## Story
**As a** gestor,
**I want** que cada ligação WhatsApp seja gravada em `calls`/`interactions` e contabilizada na produtividade do SDR,
**so that** a ligação WhatsApp apareça nas métricas de Ligação do app e no BI do Sales Hub, junto das ligações comuns.

## Complexity
**S-M** (persistência + disparo de webhook + verificação BI)

## Scope
**IN:**
- Gravar `calls`: `type='outbound'`, `metadata={provider:'whatsapp', service_call_id, service_session_id}`, `status` (disposition), `connected`, `answered_at`, `duration_seconds`, `hangup_cause`, `user_id`, `lead_id`, `started_at`.
- Gravar `interactions`: `channel='phone'`, `type='sent'`, `step_id`, `cadence_id`, `metadata.provider='whatsapp'`, `performed_by`.
- Disparar o webhook `/calls` do n8n (ou confiar no watchdog que já puxa) pra propagar ao warehouse.
- Verificar (read-only) que a ligação entra em `ligacoes_realizadas`/`ligacoes_conectadas`.

**OUT:**
- Qualquer mudança no warehouse (confirmado: zero — ver memória `calls-bi-sync-path`).
- Gravação de áudio (7.8).

## Dependencies
- 7.6 (disposition/resultado da chamada).
- SDR mapeado em `source_user_mapping` (warehouse) — pré-requisito p/ contar no BI.

## Risks
| Risco | Prob. | Impacto | Mitigação |
|-------|-------|---------|-----------|
| `type='manual'` excluído por overload BI dormente | Média | Médio | usar **`type='outbound'`** (decisão travada) |
| SDR não mapeado em `source_user_mapping` | Média | Médio | checklist de onboarding do SDR; logar skip |
| Webhook não dispara | Baixa | Médio | watchdog pg_cron já puxa periodicamente como fallback |
| Duplicação de interaction (lição API4COM) | Média | Médio | reusar dedup de interaction phone (30min) de `execute-activity` |

## Acceptance Criteria
1. Ao concluir uma ligação WhatsApp, é criada 1 linha em `calls` com `type='outbound'` e `metadata.provider='whatsapp'`.
2. `connected`/`answered_at`/`duration_seconds` refletem o lifecycle real do microserviço.
3. É criada 1 `interaction` `channel='phone'`, `type='sent'`, vinculada a `step_id`+`cadence_id` (sem duplicar).
4. A ligação aparece nas métricas internas de Ligação (dashboard/estatísticas).
5. Após o sync n8n, a ligação conta em `ligacoes_realizadas` (e `ligacoes_conectadas` se `significant` ou `≥30s`) no warehouse — verificado via SELECT read-only.
6. Sem regressão na contagem das ligações API4COM.

## Tasks / Subtasks
- [x] **Task 1**: `persistWhatsAppCall` — insere `calls` com `type='outbound'` + `metadata={provider:'whatsapp', service_session_id, service_call_id}`, `status`=disposition, `connected`, `answered_at`, `duration_seconds`
- [x] **Task 2**: insere `interaction` `channel='phone'`, `type='sent'`, ligada a `step_id`+`cadence_id`, `metadata.provider='whatsapp'`
- [x] **Task 3**: webhook **não** disparado do app — o watchdog pg_cron existente aciona a n8n "Sync Calls" (a call em `calls` é puxada sozinha)
- [x] **Task 4**: BI verificado read-only (29/jun) — `get_calls_for_v4sales(p_from_date)` **sem** filtro de `type` (`has_type_filter_ref=0`); só o overload dormente `(p_year,p_month)` filtra. `type='outbound'` conta. **Zero mudança no warehouse**
- [x] **Task 5**: 4 testes (insere call+interaction, idempotência por `service_call_id`, sem org, input inválido)

## Dev Notes
- Caminho BI confirmado: `get_calls_for_v4sales(p_from_date)` → `sync_calls_from_enriquece`. `connected` é recalculado no warehouse (`significant OR ≥30s`). Detalhes na memória `calls-bi-sync-path`.
- **Dedup adaptado:** não há interação `phone` órfã de webhook externo (a ligação nasce no app), então a idempotência é por `metadata.service_call_id` na própria `calls` (re-submit não duplica) — não o dedup de 30min do API4COM.
- **Wiring:** `CallDispositionForm` ganhou `onPersist` opcional (best-effort); o painel captura os metadados (sid/callId/duração/answered) e persiste **antes** do avanço da cadência. Falha de persistência **não** bloqueia o avanço.

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (@dev / Dex)

### File List
- `src/features/whatsapp-calls/actions/persist-call.ts` (novo) + `persist-call.test.ts` (novo)
- `src/features/whatsapp-calls/components/CallDispositionForm.tsx` (mod — prop `onPersist`)
- `src/features/whatsapp-calls/components/ActivityWhatsAppCallPanel.tsx` (mod — captura metadados + `onPersist`)
- `src/features/activities/components/ActivityExecutionSheetContent.tsx` (mod — passa `cadenceId`/`leadId`)

### Completion Notes
- `typecheck` ✓ · `lint` ✓ · **suíte completa 1455 testes** ✓ (10 RLS skipped). 4 testes novos.
- BI confirmado ao vivo (read-only) hoje: caminho ativo não filtra `type`. Sem mudança no warehouse.
- Persistência é **idempotente** (`service_call_id`) e **best-effort** (não trava a cadência).
- `calls.origin='whatsapp'` (cosmético; o BI mapeia o SDR por `user_id` via `source_user_mapping`, não por origin).
- CodeRabbit pulado (CLI WSL-only no macOS) — gate = typecheck/lint/test + CI.

## Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-06-28 | @dev | Story criada a partir do epic-7 |
| 2026-06-28 | @po (Pax) | Validada (10-point checklist) 10/10 — GO. Status Draft → Ready. |
| 2026-06-29 | @dev (Dex) | Implementada: persistência call+interaction + wiring + BI verificado. typecheck/lint/1455 ✓. Status Ready → Ready for Review. |
