# Story 7.1: Microserviço de Voz WhatsApp (fork WaCalls + deploy VPS)

## Status
Ready

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["go test", "docker", "manual smoke"]

## Story
**As a** plataforma EnriqueceAI,
**I want** um microserviço Go isolado que faça chamadas de voz 1:1 via WhatsApp (WebRTC ↔ MLow ↔ SRTP do WhatsApp),
**so that** o front do Enriquece consiga iniciar/encerrar ligações via REST sem acoplar whatsmeow/pion ao runtime Next.js.

## Complexity
**L — Large** (fork + 3 reimplementações de produção + deploy VPS com rede)

## Scope
**IN:**
- Fork do WaCalls (MIT) como repo separado proprietário.
- Auth por **API key** (`X-API-Key` header + `?apiKey=` p/ SSE) — reimplementado (referência AstraCalls, sem copiar código AGPL).
- **Webhook por sessão** (`GET/POST/DELETE /api/sessions/{sid}/webhook`) emitindo lifecycle de chamada — reimplementado.
- Fix **NAT 1:1 / ICE-TCP** (`WACALLS_PUBLIC_IP=auto`, `WACALLS_UDP_PORT`) p/ VPS que bloqueia UDP inbound.
- Deploy Docker (network=host) + HTTPS (Traefik/socat) na VPS.

**OUT:**
- Postgres por sessão estilo WAHA (SQLite do WaCalls basta no MVP de 1 número).
- API de mensagens, Chatwoot, widget.js do AstraCalls.
- Perna de IA (PCM 16kHz → Gemini/Pipecat).
- Multi-número/pool.

## Dependencies
- VPS provisionada com IP público e portas liberadas (UDP + TCP fallback).
- Decisão de licença travada (base MIT, sem importar AGPL).

## Risks
| Risco | Prob. | Impacto | Mitigação |
|-------|-------|---------|-----------|
| UDP inbound bloqueado quebra mídia WebRTC | Alta | Alto | ICE-TCP no mesmo port + `SetNAT1To1IPs` |
| Instabilidade de sessão whatsmeow | Média | Médio | endpoint de re-pareamento + SSE de status |
| Sem auth → exposição | Alta | Crítico | API key obrigatória em todas as rotas `/api/*` |
| Contaminação AGPL | Baixa | Alto | reimplementar auth/webhook/NAT, não copiar do AstraCalls |

## Acceptance Criteria
1. O serviço sobe via `docker stack deploy`/compose com `WACALLS_PUBLIC_IP`, `WACALLS_UDP_PORT`, `WACALLS_API_KEY` configurados.
2. Todas as rotas `/api/*` exigem API key válida; sem key → 401.
3. `POST /api/sessions` cria sessão e emite QR; `GET /api/sessions` reporta `paired`/status.
4. `POST /api/sessions/{sid}/calls {phone, record?}` inicia chamada e retorna `call_id`; `POST .../webrtc` troca SDP; `DELETE .../calls/{id}` encerra.
5. `GET /api/events` (SSE) emite eventos de lifecycle: `ringing`, `active/answered`, `ended`, `rejected`, `no-answer`.
6. Webhook por sessão dispara POST com o mesmo lifecycle pro destino configurado.
7. Com UDP inbound bloqueado na VPS, a chamada ainda conecta via ICE-TCP (validado num teste real).
8. Uma chamada de voz 1:1 real completa: SDR (browser) ↔ número WhatsApp de teste, áudio bidirecional.

## Tasks / Subtasks
- [ ] **Task 1**: Fork WaCalls + setup repo proprietário (licença/atribuição MIT preservada)
- [ ] **Task 2**: Implementar API key middleware (header + query p/ SSE)
- [ ] **Task 3**: Implementar webhook por sessão (config CRUD + emissão de lifecycle)
- [ ] **Task 4**: Configurar NAT 1:1 + ICE-TCP (`SetNAT1To1IPs`, port único UDP+TCP)
- [ ] **Task 5**: Dockerfile + stack file (network=host) + HTTPS
- [ ] **Task 6**: Smoke test end-to-end (pareamento + chamada real + ICE-TCP fallback)
- [ ] **Task 7**: Documentar env vars + runbook de operação

## Dev Notes
- Pure-Go, **sem cgo** (MLow vendorizado) — build simples.
- Endpoints confirmados no plano §0.4. Bridge de áudio (PCM 16kHz) só importa pra fase IA futura.

### Scaffold de referência (29/jun)
Há um **seed pronto** em `docs/services/whatsapp-voice/` (mover para o repo Go separado):
- `Dockerfile` (pure-Go, distroless), `docker-compose.yml` (network=host), `.env.example`.
- **`ENDPOINTS.md` = contrato fonte-da-verdade**, derivado do que o app-side já consome
  (`voice-service-client.ts` + a boundary `voice-call-media.ts`). O **checklist de paridade**
  no fim do `ENDPOINTS.md` é o que esta story precisa entregar para o app funcionar.
- Arquitetura: sinalização (SDP/QR) via proxy do Enriquece (injeta `X-API-Key`); **mídia
  (RTP/ICE) direto** browser ↔ serviço na `WACALLS_UDP_PORT` (NAT 1:1 + ICE-TCP).

### Estado da ativação (o resto do epic já está pronto)
- ✅ App-side (8/9 stories) mergeado; ✅ migrations de banco aplicadas em prod (29/jun).
- ⏳ **Esta story (7.1)** + configurar `WACALLS_BASE_URL`/`WACALLS_API_KEY` no Coolify
  + completar `voice-call-media.ts` (RTCPeerConnection + `/webrtc` + SSE) = o que falta
  para o end-to-end. Gates F3/F4 do QA (`docs/qa/epic-7-whatsapp-call-qa-gate.md`) fecham
  na 1ª ligação real.

## Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-06-28 | @dev | Story criada a partir do epic-7 |
| 2026-06-28 | @po (Pax) | Validada (10-point checklist) 9/10 — GO. Status Draft → Ready. Nota: infra em repo Go separado, rastrear fora do board do app. |
| 2026-06-29 | @dev (Dex) | Scaffold de referência criado (`docs/services/whatsapp-voice/`: Dockerfile + compose + .env + ENDPOINTS.md com checklist de paridade). Story permanece `Ready` (build no repo Go separado). |
