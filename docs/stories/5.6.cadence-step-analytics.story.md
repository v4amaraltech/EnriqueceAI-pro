# Story 5.6: Estatísticas por Step da Cadência

## Status

**InProgress**

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test", "build"]

## Complexity Estimate

**M (5 points)** — Novo serviço de agregação por step, componente de tabela detalhada, integração com filtros existentes, queries com GROUP BY em interactions.

## Risks

- **R1:** Queries agrupando interactions por step_id em períodos grandes podem ser lentas — mitigado: índice `idx_interactions_step_variant(step_id, type)` já existe
- **R2:** Cadências com muitos steps (10+) podem gerar tabelas extensas — mitigado: expandir/colapsar por cadência, scroll horizontal se necessário
- **R3:** Steps sem interactions no período podem não aparecer — mitigado: LEFT JOIN com cadence_steps para mostrar todos os steps mesmo com zero dados

## Dependencies

- **Story 5.1** — DateRangePicker (filtro de período)
- **Story 5.5** — AnalyticsFilters (filtros de SDR e cadência)

## Story

**As a** manager,
**I want** to see detailed performance metrics for each step of my cadences,
**so that** I can identify which steps drive engagement and optimize my outreach sequences.

## Scope

**IN:**
- Tabela detalhada por step dentro de cada cadência com métricas: Enviados, Abertos, Clicados, Respondidos, Reuniões, Taxa de Abertura %, Taxa de Resposta %
- KPI cards no topo: Total Enviados, Total Engajados (abriu/clicou/respondeu), Taxa de Engajamento %
- Drill-down: clicar em uma cadência na `CadencePerformanceTable` existente expande os steps
- Filtros existentes do `AnalyticsFilters` (período, SDR, cadência) aplicados
- Server action `fetchStepLevelAnalytics` com agregação por `step_id` + `type`
- Integração na página `/statistics/prospecting/cadences`

**OUT:**
- Métricas de A/B testing por step (story futura)
- Gráfico de funil visual por steps (mantém tabela por simplicidade)
- Estatísticas de WhatsApp engagement (WhatsApp não tem open/click tracking confiável)
- Comparação período-a-período nos steps (pode usar DeltaIndicator da 5.2 futuramente)

## Acceptance Criteria

1. Ao clicar em uma cadência na tabela existente, expande mostrando métricas por step
2. Cada step mostra: Passo #, Atividade (canal + nome), Enviados, Abertos, Clicados, Respondidos, Reuniões
3. Taxas calculadas: Abertura % (abertos/enviados), Click % (clicados/enviados), Resposta % (respondidos/enviados)
4. KPI cards no topo da página: Total Enviados, Engajados (leads únicos com open/click/reply), Taxa de Engajamento %
5. Filtros de período, SDR e cadência funcionam corretamente com os dados por step
6. Steps sem dados no período aparecem com zeros (não são omitidos)
7. Loading skeleton durante carregamento dos dados de step
8. Testes unitários para o serviço de agregação e componente de tabela
9. `pnpm typecheck`, `pnpm lint`, `pnpm test:run` e `pnpm build` passam

## Tasks / Subtasks

- [x] **Task 1: Server Action fetchStepLevelAnalytics** (AC: 1, 2, 3, 5, 6)
  - [x] 1.1 Criar `src/features/statistics/actions/fetch-step-analytics.ts`
  - [x] 1.2 Query: `interactions` GROUP BY `step_id, type` com contagem, filtrado por `org_id`, `cadence_id`, período, SDR opcional
  - [x] 1.3 LEFT JOIN com `cadence_steps` para incluir steps sem interactions
  - [x] 1.4 Calcular taxas: abertura, click, resposta, engajamento
  - [x] 1.5 Retornar `ActionResult<CadenceStepAnalyticsData>` com shape tipado

- [x] **Task 2: Tipos e interfaces** (AC: 2, 3)
  - [x] 2.1 Criar `src/features/statistics/types/step-analytics.ts` com `CadenceStepMetrics` interface
  - [x] 2.2 Campos: stepOrder, channel, activityName, sent, opened, clicked, replied, meetingScheduled, openRate, clickRate, replyRate

- [x] **Task 3: Componente CadenceStepTable** (AC: 1, 2, 3, 6, 7)
  - [x] 3.1 Criar `src/features/statistics/components/CadenceStepTable.tsx`
  - [x] 3.2 Tabela com colunas: Passo, Atividade, Enviados, Abertos (%), Clicados (%), Respondidos (%), Reuniões
  - [x] 3.3 Loading skeleton enquanto dados carregam
  - [x] 3.4 Empty state para steps sem dados
  - [x] 3.5 Formatação: taxas inline com counts

- [x] **Task 4: Integrar em CadenceAnalyticsView** (AC: 1, 4, 5)
  - [x] 4.1 Adicionar expand/collapse na `CadencePerformanceTable` — clicar na row expande steps
  - [x] 4.2 Fetch `fetchStepAnalytics` ao expandir uma cadência (lazy load + cache)
  - [x] 4.3 Adicionar KPI cards: Total Enviados, Engajados, Taxa de Engajamento %
  - [x] 4.4 Filtros lidos via `useSearchParams()` e passados para query de steps

- [x] **Task 5: Testes** (AC: 8, 9)
  - [x] 5.1 Testes para `step-analytics.service` (mock Supabase, verificar agregação, 5 tests)
  - [x] 5.2 Testes para `CadenceStepTable` (render, loading, empty state, rates, 4 tests)
  - [x] 5.3 `pnpm typecheck && pnpm test:run && pnpm build` passam

## Dev Notes

**Índice existente (performance garantida):**
```sql
CREATE INDEX idx_interactions_step_variant ON interactions (step_id, type) WHERE step_id IS NOT NULL;
```

**Query base para step analytics:**
```typescript
const { data } = await supabase
  .from('interactions')
  .select('step_id, type')
  .eq('org_id', orgId)
  .eq('cadence_id', cadenceId)
  .gte('created_at', from)
  .lte('created_at', to)
  .not('step_id', 'is', null);

// Agrupar client-side por step_id + type (Supabase JS não suporta GROUP BY)
```

**Steps data (para labels):**
```typescript
const { data: steps } = await supabase
  .from('cadence_steps')
  .select('id, step_order, channel, activity_name')
  .eq('cadence_id', cadenceId)
  .order('step_order', { ascending: true });
```

**Conceito de "Engajado" (Meetime):**
Lead que teve qualquer interação positiva no step: `type IN ('opened', 'clicked', 'replied', 'meeting_scheduled')`. Contar leads **únicos** (DISTINCT lead_id).

**CadencePerformanceTable existente:**
```
src/features/statistics/components/CadencePerformanceTable.tsx
```
Já renderiza rows por cadência — precisa adicionar expand/collapse para mostrar steps.

**Relevant Source Tree:**
```
src/features/statistics/components/CadenceAnalyticsView.tsx
src/features/statistics/components/CadencePerformanceTable.tsx
src/features/statistics/components/StepProgressionChart.tsx (já existe, mostra steps agregados)
src/features/statistics/actions/fetch-cadence-analytics.ts
src/features/statistics/services/cadence-analytics.service.ts
```

### Testing

- **Framework:** Vitest + Testing Library
- **Location:** `src/features/statistics/actions/__tests__/`, `src/features/statistics/components/__tests__/`
- **Rodar:** `pnpm vitest run src/features/statistics`
- **Mocking:** Supabase client mock padrão do projeto

## File List

### Created
- `src/features/statistics/types/step-analytics.ts` — CadenceStepMetrics + CadenceStepAnalyticsData interfaces
- `src/features/statistics/services/step-analytics.service.ts` — fetchStepAnalyticsData with client-side aggregation
- `src/features/statistics/actions/fetch-step-analytics.ts` — Server action wrapping service
- `src/features/statistics/components/CadenceStepTable.tsx` — Sub-table component for step metrics
- `src/features/statistics/services/__tests__/step-analytics.service.test.ts` — 5 service tests
- `src/features/statistics/components/__tests__/CadenceStepTable.test.tsx` — 4 component tests

### Modified
- `src/features/statistics/types/cadence-analytics.types.ts` — +totalSent, engagedLeads, engagementRate fields
- `src/features/statistics/services/cadence-analytics.service.ts` — +engagement query, +3 KPI fields, +emptyData update
- `src/features/statistics/components/CadenceAnalyticsView.tsx` — +3 engagement KPI cards (Send, UserCheck, TrendingUp)
- `src/features/statistics/components/CadencePerformanceTable.tsx` — Expand/collapse with lazy fetch, cache, chevron icons

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 1.0 | Story criada — Estatísticas por step da cadência (referência Meetime) | @qa (Quinn) |
| 2026-03-22 | 1.1 | Implementação completa — Tasks 1-5 [x], 9 novos testes, typecheck/test/build OK | @dev (Dex) |
