# Story 7.9: Anti-ban + Observabilidade do Número

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "lint"]

## Story
**As a** gestor,
**I want** limites diários por número e monitoramento da saúde da sessão WhatsApp,
**so that** eu reduza o risco de ban e detecte degradação antes de perder o número.

## Complexity
**S — Small** (limites + monitor/alerta)

## Scope
**IN:**
- Limite diário de chamadas por número (reusar padrão `controle_disparo` da Maskavo).
- Monitor de saúde: taxa de `not_connected`/erro de sessão por número, com alerta de degradação.
- Badge/indicador do status da sessão na UI (connected/disconnected/degraded).

**OUT:**
- Auto-rotação de número/pool (futuro).
- Throttling adaptativo automático (futuro — só alertar no MVP).

## Dependencies
- 7.3 (sessão/número), 7.7 (eventos de call pra computar taxas).

## Risks
| Risco | Prob. | Impacto | Mitigação |
|-------|-------|---------|-----------|
| Ban silencioso (número morre sem sinal claro) | Média | Alto | monitorar pico de `not_connected`/erro de sessão como proxy de throttle |
| Limite muito baixo trava o SDR | Baixa | Médio | limite configurável por número/org |

## Acceptance Criteria
1. Existe um teto diário de chamadas WhatsApp por número (configurável), e o sistema bloqueia/avisa ao atingir.
2. O sistema computa, por número, a taxa de `not_connected`/erro de sessão num período móvel.
3. Quando a taxa degrada acima de um limiar, dispara alerta (notificação/log) pro gestor.
4. A UI mostra o status de saúde da sessão (connected/disconnected/degraded).
5. O posicionamento operacional (reativação/aquecimento de base) está documentado no runbook.

## Tasks / Subtasks
- [x] **Task 1**: Teto por número em janela móvel de 24h — `startWhatsAppCall` bloqueia ao atingir `DAILY_CALL_LIMIT` (`controle_disparo` é da Maskavo; reimplementado aqui via `calls`)
- [x] **Task 2**: Taxa de falha por número (24h) — `computeNumberHealth` (puro) + agregação na página (`not_connected`/total)
- [x] **Task 3**: Badge de saúde na tela de Números WhatsApp (`Saudável`/`Degradado`/`Limite 24h`) + coluna "Uso (24h)". Alerta ativo (notificação) = follow-up no runbook
- [x] **Task 4**: Runbook `docs/runbooks/whatsapp-call-antiban.md`
- [x] **Task 5**: 6 testes (5 de `computeNumberHealth` + 1 do bloqueio por limite)

## Dev Notes
- Sem fonte pública de tolerância da Meta — o MVP é instrumentado pra **descobrir empiricamente** o limite seguro.
- **Janela móvel de 24h** (não dia-calendário) — evita ambiguidade de fuso. Valores em `constants.ts` (`DAILY_CALL_LIMIT=50`, `NOT_CONNECTED_ALERT_RATE=0.5`, `HEALTH_MIN_SAMPLE=5`) — calibrar na operação.
- `not_connected` (disposition) é o proxy de throttle/ban observado por número.

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (@dev / Dex)

### File List
- `src/features/whatsapp-calls/constants.ts` (mod — limites/limiares)
- `src/features/whatsapp-calls/health.ts` (novo — `computeNumberHealth` puro) + `health.test.ts` (novo)
- `src/features/whatsapp-calls/actions/calls.ts` (mod — bloqueio por limite 24h) + `calls.test.ts` (mod)
- `src/features/whatsapp-calls/types/index.ts` (mod — `usage` em `WhatsAppNumberRow`)
- `src/features/whatsapp-calls/components/WhatsAppNumbersManager.tsx` (mod — coluna Uso + badge)
- `src/app/(app)/settings/whatsapp-numbers/page.tsx` (mod — agregação 24h)
- `docs/runbooks/whatsapp-call-antiban.md` (novo)

### Completion Notes
- `typecheck` ✓ · `lint` ✓ · **suíte completa 1461 testes** ✓ (10 RLS skipped). 6 testes novos.
- Teto e limiares são **constantes globais** (MVP) — configuração por org/número é follow-up.
- Alerta de degradação é **visual** (badge) + log; notificação ativa é follow-up (runbook).
- CodeRabbit pulado (CLI WSL-only no macOS) — gate = typecheck/lint/test + CI.

## Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-06-28 | @dev | Story criada a partir do epic-7 |
| 2026-06-28 | @po (Pax) | Validada (10-point checklist) 8/10 — GO condicional. Status Draft → Ready. Nota: limiares numéricos serão calibrados empiricamente no MVP. |
| 2026-06-29 | @dev (Dex) | Implementada: limite 24h + saúde por número + badge + runbook + 6 testes. typecheck/lint/1461 ✓. Status Ready → Ready for Review. |
