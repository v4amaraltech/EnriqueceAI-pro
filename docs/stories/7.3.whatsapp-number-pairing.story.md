# Story 7.3: Pareamento de Número WhatsApp por SDR

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "lint"]

## Story
**As a** gestor (manager),
**I want** parear um número WhatsApp dedicado a cada SDR via QR code,
**so that** cada SDR tenha identidade própria de chamada e o risco de ban fique distribuído.

## Complexity
**M — Medium** (proxy server-side + QR/polling + persistência de sessão)

## Scope
**IN:**
- Server Action/route proxy que injeta a `WACALLS_API_KEY` (browser nunca fala direto com o microserviço).
- Criar sessão (`POST /api/sessions`), exibir **QR**, pollar status (`GET /api/sessions`) até `paired`.
- Persistir/atualizar `whatsapp_call_sessions` (status `disconnected|pairing|connected`).
- Re-parear sessão morta (`POST /api/sessions/{sid}/pair`).
- Tela de gestão (manager-only via `requireManager()`).

**OUT:**
- Pool multi-número.
- Auto-recuperação sem intervenção (monitor fica em 7.9).

## Dependencies
- 7.1 (microserviço com `/api/sessions*` + API key).
- 7.2 (tabela `whatsapp_call_sessions`).

## Risks
| Risco | Prob. | Impacto | Mitigação |
|-------|-------|---------|-----------|
| API key vazar pro browser | Média | Crítico | todo acesso ao serviço via proxy server-side; key só em env do servidor |
| QR expira antes do scan | Média | Baixo | polling + botão "gerar novo QR" |
| Sessão cai e SDR não percebe | Média | Médio | status persistido + badge; monitor em 7.9 |

## Acceptance Criteria
1. Manager acessa tela de "Números WhatsApp" (manager-only); SDR comum não acessa.
2. "Parear número" chama o proxy → `POST /api/sessions` → exibe QR retornado pelo serviço.
3. O sistema polla status e, ao `paired`, persiste `whatsapp_call_sessions` (`status='connected'`, `phone_number`, `service_session_id`, `paired_at`).
4. O browser **nunca** recebe a API key; toda chamada ao microserviço passa por route/action server-side.
5. Re-parear uma sessão `disconnected` gera novo QR sem criar registro duplicado.
6. Cada SDR tem no máximo 1 sessão ativa (constraint/validação).
7. RLS: SDR só vê a própria sessão; manager vê todas da org.

## Tasks / Subtasks
- [ ] **Task 1**: Route handler/Server Action proxy (injeta API key, normaliza erros do serviço)
- [ ] **Task 2**: UI de pareamento (QR + polling + estados)
- [x] **Task 1**: Proxy server-side (`voice-service-client` injeta `X-API-Key`; `pairing` actions normalizam erros via `VoiceServiceError`)
- [x] **Task 2**: UI de pareamento (`PairNumberDialog` — QR + polling de status + estados starting/awaiting/connected/error)
- [x] **Task 3**: Persistência/upsert em `whatsapp_call_sessions` (`createPairingSession`: insert ou update da linha existente)
- [x] **Task 4**: Re-pareamento de sessão morta (`repairSession` → `POST /pair`)
- [x] **Task 5**: Guard manager-only (`requireManager` nas actions + na página) + RLS de 7.2 (SDR vê a sua, manager vê todas)
- [x] **Task 6**: Testes (4) — proxy/criação, validação de target, serviço não-configurado, conexão+persistência do número

## Dev Notes
- Espelha o tratamento de instância Evolution órfã (memória `whatsapp-evolution-orphan-instances`) — cuidado com sessão duplicada ao reconectar.
- **Detalhes de fio do 7.1 isolados** em `voice-service-client.ts`: shapes de request/response, mapeamento de status (`paired`→connected etc.) e entrega do QR. Se o 7.1 entregar o QR só via SSE (provável), só este arquivo + o polling mudam.
- API key **nunca** vai ao browser — todo acesso ao serviço passa pelas Server Actions.
- Página acessível em `/settings/whatsapp-numbers` + card no índice de Settings.

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (@dev / Dex)

### File List
- `src/config/env.ts` (mod — `WACALLS_BASE_URL`/`WACALLS_API_KEY` opcionais)
- `src/features/whatsapp-calls/services/voice-service-client.ts` (novo — client isolado do serviço)
- `src/features/whatsapp-calls/actions/pairing.ts` (novo — Server Actions)
- `src/features/whatsapp-calls/actions/pairing.test.ts` (novo — 4 testes)
- `src/features/whatsapp-calls/components/PairNumberDialog.tsx` (novo — QR + polling)
- `src/features/whatsapp-calls/components/WhatsAppNumbersManager.tsx` (novo — lista/ações)
- `src/features/whatsapp-calls/types/index.ts` (mod — `WhatsAppNumberRow`)
- `src/app/(app)/settings/whatsapp-numbers/page.tsx` (novo — página manager-only)
- `src/app/(app)/settings/page.tsx` (mod — card de navegação)

### Completion Notes
- `typecheck` ✓ · `lint` ✓ · **suíte completa 1429 testes** ✓ (10 RLS skipped — Supabase down). 4 testes novos.
- **Dependência 7.1 (esperada):** o end-to-end real exige o microserviço no ar + env `WACALLS_*` + a migration de 7.2 aplicada. Sem isso, a action retorna erro amigável ("serviço não configurado"). Contrato do serviço isolado no client (ajuste pequeno quando 7.1 existir, especialmente se o QR vier por SSE).
- `react-hooks/set-state-in-effect`: resolvido extraindo o fluxo num filho `PairFlow` remontado por `key` (estado inicial via `useState`, sem reset síncrono em efeito).
- CodeRabbit pulado (CLI WSL-only no macOS) — gate = typecheck/lint/test + CI.

## Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-06-28 | @dev | Story criada a partir do epic-7 |
| 2026-06-28 | @po (Pax) | Validada (10-point checklist) 10/10 — GO. Status Draft → Ready. |
| 2026-06-28 | @dev (Dex) | Implementada: client do serviço + actions + UI manager-only + persistência + 4 testes. typecheck/lint/1429 ✓. Status Ready → Ready for Review. |
