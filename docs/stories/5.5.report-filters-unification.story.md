# Story 5.5: Report Filters Unification

## Status

**Ready**

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["lint", "typecheck", "test", "build"]

## Complexity Estimate

**S (3 points)** — Extrair componente compartilhado de filtros, migrar 2 telas, adicionar filtros faltantes em Reports.

## Risks

- **R1:** Migrar filtros de Statistics pode quebrar URL params existentes — mitigado: manter mesmos nomes de params (`sdr`, `cadence`)
- **R2:** Reports não filtra por SDR/cadência atualmente, queries precisam ser atualizadas — mitigado: queries já recebem dados que podem ser filtrados client-side ou server-side

## Dependencies

- **Story 5.1** — Custom Date Range Picker (fornece `DateRangePicker` que será incluído no `AnalyticsFilters`)

## Story

**As a** manager,
**I want** the same filter experience across Reports and Statistics,
**so that** I can switch between views without losing my filter context.

## Scope

**IN:**
- Adicionar filtro por SDR no `ReportsView` (atualmente só Statistics tem)
- Adicionar filtro por cadência no `ReportsView` (atualmente só Statistics tem)
- Componente compartilhado `AnalyticsFilters` em `src/shared/components/` com: DateRangePicker + SDR select + Cadence select
- Migrar `StatisticsFilters` para usar `AnalyticsFilters`
- Migrar `ReportsView` para usar `AnalyticsFilters`
- URL params unificados: `from`, `to`, `sdr`, `cadence`

**OUT:**
- Filtro por origem (lead source) — complexidade adicional desnecessária agora
- Filtros salvos / presets de filtro
- Filtro no Dashboard (mantém `PeriodFilter` próprio por ter UX diferente)

## Acceptance Criteria

1. `AnalyticsFilters` renderiza DateRangePicker + SDR multi-select + Cadence select
2. Reports filtra por SDR: Overall/Cadence/SDR tables refletem seleção
3. Reports filtra por cadência: tabelas mostram apenas cadência selecionada
4. Statistics continua funcionando com os mesmos filtros (sem regressão)
5. URL params compartilhados entre Reports e Statistics
6. Testes de componente para `AnalyticsFilters`
7. `pnpm typecheck`, `pnpm lint`, `pnpm test:run` e `pnpm build` passam

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

## Tasks / Subtasks

- [ ] **Task 1: Componente AnalyticsFilters** (AC: 1)
  - [ ] 1.1 Criar `src/shared/components/AnalyticsFilters.tsx`
  - [ ] 1.2 Incluir `DateRangePicker` (da 5.1) + toggle de comparação (da 5.2)
  - [ ] 1.3 Incluir SDR multi-select (reutilizar padrão do `StatisticsFilters`)
  - [ ] 1.4 Incluir Cadence select (reutilizar padrão do `StatisticsFilters`)
  - [ ] 1.5 Props: `members`, `cadences`, `showCompare?`, `basePath` (para navigation)

- [ ] **Task 2: Atualizar Reports para filtrar por SDR e Cadência** (AC: 2, 3)
  - [ ] 2.1 Atualizar `fetchReportData` para aceitar `sdrId?` e `cadenceId?`
  - [ ] 2.2 Filtrar enrollments e interactions por SDR quando selecionado
  - [ ] 2.3 Filtrar por cadência quando selecionada
  - [ ] 2.4 Passar `members` e `cadences` como props para a page de Reports

- [ ] **Task 3: Migrar ReportsView** (AC: 2, 3, 5)
  - [ ] 3.1 Substituir filtros inline de `ReportsView` por `AnalyticsFilters`
  - [ ] 3.2 Ler `sdr` e `cadence` dos searchParams na page
  - [ ] 3.3 Passar filtros para `fetchReportData`

- [ ] **Task 4: Migrar StatisticsFilters** (AC: 4, 5)
  - [ ] 4.1 Substituir `StatisticsFilters` por `AnalyticsFilters` nas pages de Statistics
  - [ ] 4.2 Garantir que todos os params existentes continuam funcionando
  - [ ] 4.3 Verificar sub-pages de Prospecting (`/statistics/prospecting/*`)

- [ ] **Task 5: Testes** (AC: 6, 7)
  - [ ] 5.1 Testes para `AnalyticsFilters` (render, SDR select, Cadence select, URL params)
  - [ ] 5.2 Testes de regressão para Reports com novos filtros
  - [ ] 5.3 Garantir `pnpm typecheck && pnpm lint && pnpm test:run && pnpm build` passam

## Dev Notes

**StatisticsFilters existente (referência):**
```
src/features/statistics/components/StatisticsFilters.tsx
```
Contém: period select, SDR multi-select (via `organization_members`), cadence select. Usa `useSearchParams` e `router.push`.

**Members e cadences na page de Statistics:**
A page server-side já busca members e cadences para passar como props. Reports precisa fazer o mesmo.

**Reports page atual:**
```
src/app/(app)/reports/page.tsx
```
Passa `result` e `hasFilters` para `ReportsView`. Precisa adicionar fetch de `members` e `cadences` aqui.

**URL params unificados:**

| Param | Tipo | Descrição |
|-------|------|-----------|
| `from` | ISO date | Início do período |
| `to` | ISO date | Fim do período |
| `compare` | boolean | Comparação ativa |
| `sdr` | UUID | Filtrar por SDR (pode ser multi-value) |
| `cadence` | UUID | Filtrar por cadência |

**Relevant Source Tree:**
```
src/features/statistics/components/StatisticsFilters.tsx (fonte)
src/features/statistics/components/ProspectingSidebarFilters.tsx
src/features/reports/components/ReportsView.tsx (destino)
src/app/(app)/reports/page.tsx (page server-side)
src/app/(app)/statistics/page.tsx
src/app/(app)/statistics/prospecting/layout.tsx
src/shared/components/DateRangePicker.tsx (da 5.1)
```

### Testing

- **Framework:** Vitest + Testing Library
- **Location:** `src/shared/components/__tests__/`
- **Rodar:** `pnpm vitest run src/shared/components`
- **Mocking:** `useSearchParams`, `useRouter` de next/navigation

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-18 | 1.0 | Story criada a partir da Epic 5 | @sm (River) |
| 2026-03-18 | 1.1 | Validação GO (10/10) — Status Draft → Ready | @po (Pax) |
