# Story 7.4: Passo "Ligação via WhatsApp" no Builder de Cadência

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "lint"]

## Story
**As a** gestor montando uma cadência,
**I want** adicionar um passo "Ligação via WhatsApp" que persista como ligação com provider WhatsApp,
**so that** o SDR receba esse passo na fila e o discador WhatsApp seja acionado na execução.

## Complexity
**S — Small** (evolui o item de sidebar já existente)

## Scope
**IN:**
- Passo persiste como `channel='phone'` + `call_provider='whatsapp'` (via 7.2).
- Item "Ligação via WhatsApp" na sidebar (categoria Ligação) carrega o provider no drop.
- `StepEditorDialog` reconhece e exibe o provider; persiste `call_provider` no save.
- Gate de elegibilidade: passo só fica executável se `resolveWhatsAppPhone(lead) ≠ ∅` e `whatsapp_invalid_at IS NULL`.

**OUT:**
- Discador/execução (7.5).
- Métricas (7.7).

## Dependencies
- 7.2 (`call_provider`).

## Risks
| Risco | Prob. | Impacto | Mitigação |
|-------|-------|---------|-----------|
| Passo WhatsApp cai pra lead sem WhatsApp | Média | Médio | gate via `resolveWhatsAppPhone` + `whatsapp_invalid_at` (já existe pra channel whatsapp) |
| Confusão com a "WhatsApp Ligação" já adicionada na sidebar | Alta | Baixo | unificar: o item da sidebar passa a setar `call_provider='whatsapp'` |

## Acceptance Criteria
1. Arrastar "Ligação via WhatsApp" pro timeline cria um passo `channel='phone'` com `call_provider='whatsapp'`.
2. O `call_provider` é persistido ao salvar a cadência e relido ao editar.
3. `StepEditorDialog` distingue visualmente o passo WhatsApp (label/ícone) de uma ligação comum.
4. Na fila do SDR, o passo WhatsApp só aparece como executável quando o lead tem número WhatsApp resolvível e `whatsapp_invalid_at IS NULL`.
5. Ligação comum (`call_provider IS NULL`) continua funcionando igual (sem regressão).
6. `pnpm typecheck && pnpm lint && pnpm test:run` passam.

## Tasks / Subtasks
- [x] **Task 1**: Sidebar/drag carrega `call_provider` no payload (item + clones via "+") — `ActivityTypeItem.callProvider`, threading por `onAdd`/`addItemByChannel`/`handleCategoryAdd`
- [x] **Task 2**: Persistir `call_provider` ponta-a-ponta — `TimelineStep.callProvider`, `stepsToDays`/`daysToStepInputs` (CadenceBuilder), `TimelineStepInput` + insert (save-timeline-steps)
- [x] **Task 3**: `StepEditorDialog` distingue "ligação via WhatsApp" no título
- [x] **Task 4**: Gate em `fetch-pending-activities` — passo `phone`+`call_provider='whatsapp'` só executável com `resolveWhatsAppPhone(lead) ≠ null` e `whatsapp_invalid_at IS NULL`
- [x] **Task 5**: Testes — persistência (`save-timeline-steps.test`) + 3 do gate (`fetch-pending-activities.test`)

## Dev Notes
- O item "WhatsApp Ligação" já existia na sidebar (`channel='phone'`); esta story o **upgrada** pra carregar `call_provider='whatsapp'`.
- Drop lê o provider do payload do drag (`active.data.current.callProvider`), não do estado de overlay — reordenar passos existentes preserva o provider no próprio objeto do step.
- Gate reusa `resolveWhatsAppPhone` (que inclui fallback pra `lead.telefone`): "número resolvível" = qualquer número discável.

## Dev Agent Record

### Agent Model Used
claude-opus-4-8 (@dev / Dex)

### File List
- `src/features/cadences/components/ActivityTypeSidebar.tsx` (mod)
- `src/features/cadences/components/CadenceTimeline.tsx` (mod)
- `src/features/cadences/components/CadenceBuilder.tsx` (mod)
- `src/features/cadences/components/StepEditorDialog.tsx` (mod)
- `src/features/cadences/actions/save-timeline-steps.ts` (mod)
- `src/features/activities/actions/fetch-pending-activities.ts` (mod)
- `src/features/cadences/actions/save-timeline-steps.test.ts` (mod — teste)
- `src/features/activities/actions/fetch-pending-activities.test.ts` (mod — teste)

### Completion Notes
- `typecheck` ✓ · `lint` ✓ · **suíte completa 1425 testes** ✓ (10 RLS skipped — Supabase local down). 4 testes novos.
- Mudanças aditivas: `call_provider`/`callProvider` opcionais; sem impacto em passos/ligações existentes.
- Execução do discador (painel WebRTC) e branch visual na fila ficam para a **7.5** (escopo OUT desta story).
- CodeRabbit pulado (CLI é WSL-only no ambiente macOS) — gate = typecheck/lint/test + CI.

## Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-06-28 | @dev | Story criada a partir do epic-7 |
| 2026-06-28 | @po (Pax) | Validada (10-point checklist) 10/10 — GO. Status Draft → Ready. |
| 2026-06-28 | @dev (Dex) | Implementada: provider ponta-a-ponta no builder + gate na fila + 4 testes. typecheck/lint/1425 testes ✓. Status Ready → Ready for Review. |
